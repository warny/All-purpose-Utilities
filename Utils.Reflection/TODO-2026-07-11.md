# Utils.Reflection — Audit complémentaire (2026-07-11)

Troisième et quatrième relectures statiques de `Utils.Reflection`, complémentaires au journal historique conservé dans `TODO.md`. Ces passes se concentrent sur le protocole du worker Emit, l'isolation effective, les limites de ressources, la validation du contrat JSON, le cycle de vie des handles, les proxies et le round-robin. Aucun point ci-dessous n'est corrigé par ce commit.

## Priorité critique

### 37. Le chemin « isolé par défaut » peut retomber silencieusement sur un worker non sandboxé

`EmitWorkerProcess.StartWorkerProcess` tente d'abord `sandbox.StartProcess`, mais intercepte toute exception, détruit le conteneur puis relance exactement le même worker comme simple processus enfant. L'appelant reçoit pourtant le même proxy et aucune indication que l'isolation promise n'est plus active.

**Risque :** une panne de configuration de Bubblewrap, sandbox-exec ou AppContainer transforme silencieusement une frontière de sécurité en exécution avec les droits complets de l'utilisateur. Ce comportement contredit la présentation de `LibraryMapper.Emit<TInterface>` comme chemin sûr par défaut.

**Correction :** adopter un comportement fail-closed par défaut. Introduire une politique explicite, par exemple `SandboxFallbackPolicy.Fail`, `AllowUnsandboxedWithWarning`, et exposer dans le résultat/diagnostic le niveau réel d'isolation. Le mode non sandboxé doit demander un acquittement aussi explicite que `EmitInProcess`.

**Priorité : P0.**

### 38. Le token de méthode reçu n'est pas limité aux méthodes de l'interface chargée

Le worker résout directement `MethodMetadataToken` avec `InterfaceType.Module.ResolveMethod`, caste le résultat en `MethodInfo`, puis l'invoque. Il ne vérifie pas que la méthode résolue appartient à l'interface chargée, à une interface héritée autorisée, ni qu'elle correspond à l'une des méthodes validées au chargement.

**Risque :** tout client capable d'écrire sur le pipe peut fournir le token d'une autre méthode du même module. Une méthode statique peut notamment être invoquée sans dépendre de l'instance mappée. La faiblesse est particulièrement importante sur Unix, où l'identité du client du pipe n'est actuellement pas vérifiée.

**Correction :** construire au chargement une table immuable `requestMethodId -> MethodInfo` uniquement à partir de `interfaceType.GetMethods()`. Les appels doivent utiliser cet identifiant logique, jamais un token de métadonnées arbitraire. À défaut, vérifier strictement le module, le type déclarant, l'appartenance au graphe d'interfaces et la signature avant invocation.

**Priorité : P0.**

## Priorité haute

### 39. Les lignes JSON et les payloads du protocole n'ont aucune limite de taille

Les deux extrémités utilisent `ReadLine`/`WriteLine` et sérialisent intégralement arguments, tableaux, valeurs de retour, erreurs et stacks. Aucun plafond n'est appliqué à la longueur d'une ligne, au nombre d'arguments, à la taille des tableaux ou à la profondeur JSON réellement désérialisée.

**Risque :** consommation mémoire incontrôlée dans le processus hôte ou le worker, blocage du GC et déni de service. Une bibliothèque native ou un appelant peut produire une réponse/entrée très volumineuse bien avant le timeout temporel.

**Correction :** remplacer le framing par `longueur + payload` avec taille maximale vérifiée avant allocation, ou utiliser un lecteur borné. Ajouter des limites configurables pour requête, réponse, argument et valeur de retour, ainsi qu'une profondeur JSON explicite.

**Priorité : P1.**

### 40. Le worker crée un `Task.Run` par requête sans limitation de concurrence

Chaque ligne valide non-Shutdown est immédiatement dispatchée par `Task.Run` et conservée dans un `ConcurrentBag<Task>`. Il n'existe ni file bornée, ni sémaphore, ni quota par handle ou par worker.

**Risque :** un producteur rapide peut créer un nombre arbitraire de tâches et d'appels natifs simultanés, saturer le thread pool, la mémoire et les ressources natives. Les timeouts côté hôte abandonnent l'attente mais n'annulent pas les appels déjà lancés, ce qui permet l'accumulation de travaux orphelins.

**Correction :** utiliser une file bornée et un niveau de parallélisme configurable. Rejeter ou mettre en attente de façon bornée les requêtes excédentaires. Prévoir éventuellement une limite distincte par handle pour les bibliothèques non thread-safe.

**Priorité : P1.**

### 41. Un timeout n'annule pas le travail distant et le worker reste réutilisable sans politique d'épuisement

Après timeout, l'entrée `pending` est supprimée mais l'appel natif continue dans le worker. Sa réponse tardive est ignorée. Une succession de timeouts peut donc laisser de nombreux appels longs ou bloqués actifs tout en acceptant de nouvelles requêtes.

**Risque :** épuisement progressif du worker, effets de bord tardifs invisibles pour l'appelant et impossibilité de savoir si une opération a réellement été exécutée. Pour une fonction native non idempotente, le timeout ne signifie pas échec d'exécution.

**Correction :** documenter explicitement la sémantique « résultat inconnu après timeout ». Ajouter une politique configurable : conserver le worker, tuer le worker exclusif, recycler un worker de pool, ou plafonner le nombre d'appels abandonnés. Pour les pools, marquer le worker dégradé et le remplacer lorsque le seuil est dépassé.

**Priorité : P1.**

### 42. La validation des structs ne correspond pas au contrat réel de `System.Text.Json`

`CrossProcessMarshaling.IsSupportedType` inspecte tous les champs d'instance, publics et non publics, puis considère le struct compatible. Toutefois `JsonSerializerOptions.IncludeFields = true` n'inclut que les champs publics ; les champs privés ne sont pas sérialisés par cette option. Par ailleurs, les propriétés publiques restent sérialisées même si la validation ne les examine pas.

**Risque :** des structs sont acceptés mais perdent silencieusement leur état privé, ou exposent au contraire des propriétés publiques dont le type n'a pas été validé. Le contrat déclaré « tout état atteignable est représentable » n'est donc pas garanti.

**Correction :** définir un contrat de transport explicite. Le plus sûr est de n'accepter que des DTO publics simples dont tous les membres sérialisés sont déterminés par `JsonTypeInfo`, puis de valider exactement ces membres. Ajouter un test réel sérialisation-désérialisation pour les structs acceptés, notamment champs privés, propriétés calculées, readonly structs et layouts explicites.

**Priorité : P1.**

### 45. `EmitRoundRobin` ne garantit pas l'absence de concurrence dans une même bibliothèque native

Le commentaire public affirme que plusieurs processus évitent entièrement l'exigence de thread-safety de la bibliothèque native. Pourtant le proxy choisit simplement le worker suivant pour chaque appel, sans vérifier qu'il est libre. Dès que le nombre d'appels simultanés dépasse `workerCount`, le cycle revient sur un worker encore occupé. Chaque `EmitWorkerProcess` accepte en outre plusieurs requêtes en vol et le worker les exécute avec `Task.Run`.

**Risque :** une bibliothèque explicitement choisie pour son absence de thread-safety peut tout de même être appelée simultanément dans le même processus et sur la même instance native. Cela peut provoquer corruption d'état, crash natif ou résultats nondéterministes.

**Correction :** soit sérialiser strictement les appels par worker avec un sémaphore d'un appel en vol, soit utiliser une file de workers disponibles et attendre qu'un worker soit libéré avant de l'affecter. Si le parallélisme intra-worker reste voulu, corriger immédiatement la documentation et renommer l'API pour ne plus promettre cette garantie.

**Priorité : P1 fonctionnel et sécurité native.**

### 46. Les délégués mappés restent appelables après déchargement de la bibliothèque

`LibraryMapper.Dispose` appelle `NativeLibrary.Free`, mais les propriétés et champs délégués restent inchangés. Tout code qui conserve une référence au délégué, ou qui appelle encore un membre du mapper après `Dispose`, peut exécuter un pointeur vers une bibliothèque déjà déchargée.

**Risque :** violation d'accès et crash du processus, sans exception managée fiable.

**Correction :** définir un état disposé explicite et empêcher tout nouvel appel après déchargement. Pour le chemin `Create<T>`, générer ou fournir des wrappers qui vérifient cet état avant l'appel natif, puis vider les membres mappés lors du `Dispose`. Documenter qu'une copie externe d'un délégué natif ne doit pas survivre au mapper, ou ne plus exposer directement ces délégués.

**Priorité : P1.**

### 47. Le chargement et le mapping d'une bibliothèque ne sont pas transactionnels

`MapLibraryToInstance` charge d'abord la DLL puis affecte les membres un par un. Si un export est absent, si un type de membre est invalide ou si `SetValue` échoue, le handle reste attaché à un objet partiellement initialisé et sa libération dépend du finaliseur. Certains membres peuvent déjà pointer vers la DLL alors que la création globale a échoué.

**Risque :** fuite temporaire ou durable de handle natif, état partiellement mappé et comportement difficile à diagnostiquer.

**Correction :** résoudre et construire tous les délégués dans une phase préparatoire sans muter l'objet, puis effectuer le commit des affectations seulement lorsque tout est validé. En cas d'exception après `NativeLibrary.Load`, libérer immédiatement le handle dans un `catch/finally` et remettre l'objet dans un état vierge.

**Priorité : P1.**

### 48. Le cycle `EmitWorkerPool.Emit` n'est pas atomique vis-à-vis de `Dispose`

`GetOrStartWorker` protège seulement l'obtention du worker par `gate`; le chargement de l'interface et la création du proxy ont lieu après la sortie du verrou. Un autre thread peut donc disposer le pool et tuer le worker entre ces étapes. Inversement, un handle peut être chargé juste avant ou pendant la destruction du pool, puis être attaché à un proxy déjà inutilisable.

**Risque :** échecs nondéterministes, proxies retournés sur un worker mort et ressources chargées sans propriétaire clair.

**Correction :** maintenir une opération d'acquisition atomique avec le cycle de vie du pool, par exemple via un compteur de locations/opérations en cours et un état `Disposing`, ou conserver le verrou jusqu'à ce que le handle soit attaché au proxy. Définir précisément si `Dispose` attend les émissions en cours ou les annule.

**Priorité : P1 concurrence.**

### 49. Les proxies peuvent être disposés pendant un appel sans coordination

`EmitWorkerProxy` et `EmitWorkerRoundRobinProxy` remplacent simplement leur référence au worker ou leur tableau de membres pendant `Dispose`. Aucun verrou ni compteur d'appels actifs ne protège la séquence sélection du worker → invocation. Un `Dispose` concurrent peut donc décharger un handle ou tuer un processus pendant qu'un autre thread utilise encore ce même proxy.

**Risque :** interruption brutale d'appels natifs, erreurs de pipe et races de déchargement similaires à un appel de fonction pendant `NativeLibrary.Free`.

**Correction :** introduire un lease par invocation : empêcher les nouveaux appels dès le début du `Dispose`, attendre les appels actifs avant l'unload/kill, puis libérer les ressources. Prévoir une variante de fermeture forcée distincte si l'attente n'est pas souhaitée.

**Priorité : P1 concurrence.**

## Priorité moyenne

### 43. Les messages JSON malformés sont ignorés au lieu de fermer le protocole

Le worker et la boucle de lecture hôte avalent `JsonException` et continuent. L'émetteur de la requête concernée ne reçoit aucune réponse et attend jusqu'au timeout. La connexion reste ensuite considérée utilisable.

**Risque :** diagnostic trompeur, désynchronisation logique et répétition de timeouts. Sur une connexion supposée privée et fiable, une ligne malformée indique plutôt une corruption, une incompatibilité de version ou un client illégitime.

**Correction :** traiter une erreur de framing/désérialisation comme fatale pour la connexion, échouer toutes les requêtes en attente et arrêter le worker. Si une tolérance est réellement voulue, renvoyer une réponse d'erreur corrélable et imposer un quota très faible d'erreurs.

**Priorité : P2.**

### 44. Le compteur d'identifiants de requêtes peut réutiliser une valeur encore en vol après débordement

`nextId` est un `int` incrémenté par `Interlocked.Increment`. Après débordement, les valeurs négatives sont acceptées et, après un cycle complet, un identifiant peut théoriquement entrer en collision avec une requête toujours présente dans `pending`.

**Correction :** utiliser un `long` dans le protocole, ou allouer les identifiants en vérifiant l'absence dans `pending` avec une boucle bornée. Refuser `0` si cette valeur sert de sentinelle.

**Priorité : P3.**

### 50. La création de proxies peut laisser des handles ou workers sans propriétaire en cas d'échec tardif

Dans `EmitWorkerPool.Emit`, le handle est chargé avant `DispatchProxy.Create` et aucune compensation n'est effectuée si la création ou l'attachement du proxy échoue. Dans `EmitRoundRobin`, les workers sont nettoyés si leur démarrage échoue, mais pas si `DispatchProxy.Create` ou `AttachWorkers` échoue après la boucle de création.

**Risque :** handle conservé jusqu'à l'arrêt du pool ou processus workers abandonnés jusqu'au GC/à la fin du processus.

**Correction :** envelopper toute la phase création + attachement dans un `try/catch`. Sur échec, appeler `UnloadInterface(handle)` pour le pool et disposer tous les workers créés pour le round-robin.

**Priorité : P2.**

### 51. La documentation des timeouts contredit le comportement actuel

La documentation publique de `LibraryMapper.Emit` affirme encore que le worker est tué et que le proxy devient inutilisable après timeout. Le protocole actuel retire seulement la requête de `pending`, laisse l'appel distant continuer et conserve le worker pour les appels suivants.

**Risque :** l'appelant peut prendre des décisions erronées sur l'idempotence, le recyclage du worker et l'état d'une opération native après timeout.

**Correction :** mettre la documentation publique, le README et les exceptions en cohérence avec l'item 41. Un timeout doit être décrit comme un résultat indéterminé tant que le worker n'est pas volontairement recyclé.

**Priorité : P2 documentation contractuelle.**

## Duplications d'intention à réduire

- La politique d'isolation est dispersée entre `ProcessContainerFactory`, les trois conteneurs et le fallback spécifique de `EmitWorkerProcess`. Centraliser le résultat dans un objet décrivant le niveau réel d'isolation et la politique de repli.
- La validation du contrat de méthode est partagée entre `CrossProcessMarshaling`, la génération C# et la résolution dynamique côté worker. Produire une description de contrat unique au chargement et l'utiliser pour génération, transport et invocation.
- Les limites temporelles existent, mais les limites de volume et de concurrence sont absentes. Les regrouper dans un objet `EmitWorkerLimits` commun au process exclusif, au pool et au round-robin.
- Le cycle de vie des appels, handles et workers est réimplémenté dans le proxy exclusif, le pool et le round-robin. Introduire une abstraction commune de lease/fermeture qui empêche les unloads concurrents avec des appels actifs.
- La résolution des exports et l'affectation des membres sont mêlées dans `MapLibraryToInstance`. Séparer découverte, validation, préparation des délégués et commit transactionnel.

## Tests requis

- Forcer l'échec de `sandbox.StartProcess` et vérifier que le mode par défaut échoue sans lancer de processus non sandboxé.
- Envoyer le token d'une méthode du même module mais extérieure à l'interface et vérifier son rejet avant invocation.
- Fuzzer le protocole avec lignes très longues, JSON profondément imbriqué, tableaux volumineux et réponses surdimensionnées.
- Envoyer plus de requêtes concurrentes que la limite et vérifier backpressure/rejet déterministe sans croissance non bornée du nombre de tâches.
- Enchaîner des appels qui timeoutent et vérifier la politique de recyclage/arrêt du worker.
- Tester des structs à champs privés, champs publics, propriétés publiques, readonly fields, explicit layout et types non sérialisables dans une propriété.
- Vérifier qu'une ligne JSON malformée ferme la connexion et échoue immédiatement toutes les requêtes en vol.
- Tester le compteur d'identifiants près de `int.MaxValue` ou après migration vers `long`.
- Lancer plus d'appels simultanés que le nombre de workers round-robin et vérifier qu'aucun worker n'exécute deux appels natifs en parallèle lorsque cette garantie est demandée.
- Copier un délégué depuis un mapper, disposer le mapper puis vérifier qu'aucun appel natif n'est possible après déchargement.
- Provoquer chaque échec possible après `NativeLibrary.Load` et vérifier la libération immédiate du handle et l'absence d'affectation partielle.
- Faire courir `EmitWorkerPool.Emit` contre `Dispose` de façon répétée et vérifier qu'aucun proxy mort ni handle orphelin n'est retourné.
- Faire courir un appel long contre `Dispose` sur les trois types de proxy et vérifier la politique de drain ou d'annulation choisie.
- Forcer l'échec de `DispatchProxy.Create`/attachement et vérifier le nettoyage du handle ou des workers déjà créés.
- Vérifier que la documentation et les tests de timeout reflètent la sémantique réelle « exécution distante potentiellement toujours en cours ».

## Ordre conseillé

| Priorité | Action |
|---|---|
| P0 | Supprimer le fallback sandboxé → non sandboxé implicite |
| P0 | Remplacer les metadata tokens libres par une table de méthodes autorisées |
| P1 | Borner framing, payloads et concurrence |
| P1 | Corriger la garantie de non-concurrence du round-robin |
| P1 | Définir la politique des appels distants après timeout |
| P1 | Sécuriser le cycle de vie appels/proxies/handles et les unloads |
| P1 | Rendre le mapping natif transactionnel et bloquer les appels après déchargement |
| P1 | Aligner validation des types et contrat JSON réel |
| P2 | Nettoyer les ressources lors des échecs tardifs de création de proxy |
| P2 | Rendre les erreurs de protocole fatales et diagnostiquées |
| P2 | Corriger la documentation contractuelle des timeouts |
| P3 | Élargir/sécuriser les identifiants de requêtes |

## Avertissement de déploiement

Tant que les items 37, 38, 45 et 46 ne sont pas corrigés, ne pas considérer `LibraryMapper.Emit<TInterface>` ou `EmitRoundRobin<TInterface>` comme une frontière de sécurité ou de concurrence absolue : un échec de lancement du sandbox peut provoquer une exécution non isolée, le protocole accepte des tokens de méthodes non bornés à l'interface, le round-robin peut appeler simultanément une même instance native et les délégués directs peuvent survivre au déchargement de leur DLL. Sur Linux/macOS, cette réserve est renforcée par l'absence actuelle de vérification de l'identité du client du pipe.